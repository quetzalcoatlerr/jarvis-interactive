## OS dependencies (Ubuntu/Kubuntu)
Before installing pip packages it's nessesary to install system libraries connected to audio and speech sinthesys:
```bash
sudo apt update
sudo apt install portaudio19-dev python3-pyaudio espeak-ng xclip
```
